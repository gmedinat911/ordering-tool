name: Keep Render Server Awake

on:
  schedule:
    - cron: '*/5 * * * *'  # every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render Healthcheck
        run: curl -s https://whatsapp-cocktail-bot.onrender.com/health
